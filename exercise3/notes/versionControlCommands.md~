#**Version Control Commands**

|       Command          |                             Action				         |
|------------------------|-----------------------------------------------------------------------|
|  git init              | creates an empty Git repository					 |
|  git stage <arg> 	 | add file contents to the staging area				 |
|  git add .             | adds a change in working directory to the staging area                |
|  git commit -m "notes" | performs a commit of files edited/deleted/added to working repository |
|  git push   	 	 | updates the remote branch with local commits, publishing to GitHub    |
|  git log		 | shows the commit logs     	  		 	       		 |
