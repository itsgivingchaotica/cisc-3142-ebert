name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  exercise4:

    runs-on: macos-latest

    steps:
    - name: checkout repo
    - uses: actions/checkout@v3
    - name: run programs
      run: make exercise4
